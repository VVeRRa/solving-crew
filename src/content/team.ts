export type TeamMember = {
  name: string;
  role: string;
  bio?: string;
  linkedin?: string;
};

export const team: TeamMember[] = [
  // ÚNOR: doplň sem Martina a další (teď nech prázdné / anonymní)
];
